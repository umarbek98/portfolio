# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Build directory (not deployed to Hostinger)
# Note: We copy dist contents to root for deployment
dist/

# Environment variables (add actual values in Hostinger panel)
.env
.env.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Deployment artifacts (these are tracked for Hostinger)
# index.html - production HTML file (tracked)
# assets/ - production JS/CSS files (tracked)
# .htaccess - server configuration (tracked)

# Local deployment scripts (not pushed to repo)
deploy.sh
deploy-portfolio.sh
DEPLOY.md